<%= form_for @patient do |f| %>
	<% if @patient.errors.any? %>
		<div id="errors">
			<h2><%= pluralize(@patient.errors.count, "error") %> prevented this patient from saving:</h2>
			<ul>
				<% @patient.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<%= f.label :userId, "Usuário" %><br>
	<%= f.collection_select :id, User.all, :id, :email %>
	<br>

	<%= f.label :motherName, "" %>
	<%= f.text_field :motherName, placeholder:"",
	required:"", class:"form-control" %>
	<br>

	<%= f.label :fatherName, "" %>
	<%= f.text_field :fatherName, placeholder:"",
	class:"form-control" %>
	<br>

	<%= f.label :occupation, "" %>
	<%= f.text_field :occupation, placeholder:"",
	required:"", class:"form-control" %>
	<br>

	<%= f.label :observation, "" %>
	<%= f.text_area :observation, placeholder:"",
	class:"form-control" %>
	<br>

	<%= f.submit "Ok" %>
<% end %>
